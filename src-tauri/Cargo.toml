[package]
name = "matugen-studio"
version = "2.0.0-rc.1"
description = "A desktop app for generating and applying Matugen themes on Linux."
authors = ["raellx22"]
license = "GPL-2.0-or-later"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "matugen_studio_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["protocol-asset", "tray-icon", "image-png"] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
matugen-core = { path = "../matugen-core" }
material-colors = { version = "0.4.0", features = ["image", "serde"] }
tauri-plugin-dialog = "2.0.0"
tauri-plugin-clipboard-manager = "2.0.0"
dirs = "5.0"
execute = "0.2.15"
toml = "0.8.8"
image = { version = "0.24.9", features = ["webp", "jpeg", "png"] }
sha2 = "0.10.8"
base64 = "0.22"
md5 = "0.7"
percent-encoding = "2"
png = "0.17"
tokio = { version = "1", features = ["time", "sync"] }
zbus = "5.15"
regex = "1"
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }

# Isolated 2025 renderer; keep the existing core on material-colors 0.4.2.
material-colors-modern = { package = "material-colors", git = "https://github.com/Aiving/material-colors.git", rev = "3fe7f52cde8c221d9a44dec84bca3706a71bb080" }

[dev-dependencies]
tempfile = "3"
